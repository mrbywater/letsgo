1) задание было не только разнести css в отдельностоящие файлы, но и переписать его в формате sass, если хз как - будем созваниваться разбираться
2) файл спрайта не подключен, в css спрайта некорректный путь к картинкам
3) использовать спрайт, слегка измени компонент main-button, вместо картинки - div с background от спрайта
4) стрелка это отдельный компонент, для нее мы тоже делаем отдельный css файл
5) удали неиспользуемые файлы
6) переменные вынеси с отдельный scss _variables файл, импортируй его в основной